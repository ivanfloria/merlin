
import { Container } from "../styles/EpisodesInfoStyles"

const EpisodesInfo = ({episodes}) => {
    return(
        <Container>
            Episodes: {episodes}
        </Container>
    )
}

export default EpisodesInfo